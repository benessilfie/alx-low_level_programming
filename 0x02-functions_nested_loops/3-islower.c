#include "main.h"
/**
 * main - Entry point
 * Description: A function that checks for lowercase character.
 * Return: Always 0 (Success)
 */
int _islower(int lowerCase)
{
return (lowerCase >= 97 && lowerCase <= 122);
}
