#include "main.h"
/**
 * main - Entry point
 * Description: A function that computes the
 * absolute value of an integer.
 * Return: Always 0 (Success)
 */
int _abs(int n)
{
return (n >= 0 ? n : n * -1);
}
